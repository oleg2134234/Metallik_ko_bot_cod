import { integer, sqliteTable, text, index } from 'drizzle-orm/sqlite-core';

export const contacts = sqliteTable('contacts', { id: integer('id').primaryKey({ autoIncrement: true }), name: text('name').notNull(), company: text('company'), email: text('email'), phone: text('phone'), city: text('city'), source: text('source').notNull().default('website'), createdAt: integer('created_at', { mode: 'timestamp' }).notNull() }, (table) => [index('idx_contacts_email').on(table.email)]);
export const conversations = sqliteTable('conversations', { id: integer('id').primaryKey({ autoIncrement: true }), contactId: integer('contact_id').notNull().references(() => contacts.id), channel: text('channel').notNull(), status: text('status').notNull().default('open'), botMode: text('bot_mode').notNull().default('active'), lastMessageAt: integer('last_message_at', { mode: 'timestamp' }).notNull() }, (table) => [index('idx_conversations_status_last').on(table.status, table.lastMessageAt)]);
export const messages = sqliteTable('messages', { id: integer('id').primaryKey({ autoIncrement: true }), conversationId: integer('conversation_id').notNull().references(() => conversations.id), sender: text('sender').notNull(), body: text('body').notNull(), intent: text('intent'), createdAt: integer('created_at', { mode: 'timestamp' }).notNull() }, (table) => [index('idx_messages_conversation_created').on(table.conversationId, table.createdAt)]);
export const leads = sqliteTable('leads', { id: integer('id').primaryKey({ autoIncrement: true }), contactId: integer('contact_id').notNull().references(() => contacts.id), conversationId: integer('conversation_id').references(() => conversations.id), product: text('product').notNull(), status: text('status').notNull().default('draft'), parametersJson: text('parameters_json').notNull().default('{}'), nextAction: text('next_action'), assignedTo: text('assigned_to'), createdAt: integer('created_at', { mode: 'timestamp' }).notNull() }, (table) => [index('idx_leads_status_created').on(table.status, table.createdAt)]);
export const campaigns = sqliteTable('campaigns', { id: integer('id').primaryKey({ autoIncrement: true }), name: text('name').notNull(), subject: text('subject').notNull(), body: text('body').notNull(), status: text('status').notNull().default('draft'), sentAt: integer('sent_at', { mode: 'timestamp' }), createdAt: integer('created_at', { mode: 'timestamp' }).notNull() });
export const campaignRecipients = sqliteTable('campaign_recipients', { id: integer('id').primaryKey({ autoIncrement: true }), campaignId: integer('campaign_id').notNull().references(() => campaigns.id), contactId: integer('contact_id').notNull().references(() => contacts.id), deliveryStatus: text('delivery_status').notNull().default('queued'), repliedAt: integer('replied_at', { mode: 'timestamp' }) }, (table) => [index('idx_recipients_replied').on(table.campaignId, table.repliedAt)]);
